name: Deploy to Render

on:
  push:
    branches:
      - main  # Se activará cuando hagas push a la rama front

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '20.x'

      - name: Install dependencies
        run: npm install

      - name: Build the project
        run: npm run build

      - name: Deploy to Render
        run: |
          curl -X POST -H "Authorization: Bearer ${{ secrets.RENDER_API_KEY }}" \
          -H "Content-Type: application/json" \
          -d '{"serviceId": "rnd_5UnsHRbLM387j47PFeJh5IEjHJXT", "branch": "front"}' \
          https://api.render.com/v1/services/rnd_5UnsHRbLM387j47PFeJh5IEjHJXT/deploys
        env:
          RENDER_API_KEY: ${{ secrets.RENDER_API_KEY }}
